apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: revplay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: revplaystreaming-user-service:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: revplay-config
          env:
            - name: CONFIG_SERVER_URL
              value: "http://config-server:8888/"
            - name: EUREKA_URI
              value: "http://discovery-server:8761/eureka/"
            - name: EUREKA_HOSTNAME
              value: "user-service"
            - name: MYSQL_HOST
              value: "mysql"
          ports:
            - containerPort: 8081
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 40
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: revplay
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
