apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: revplay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
        - name: analytics-service
          image: revplaystreaming-analytics-service:v8
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: revplay-config
          env:
            - name: CONFIG_SERVER_URL
              value: "http://config-server:8888/"
            - name: EUREKA_URI
              value: "http://discovery-server:8761/eureka/"
            - name: EUREKA_HOSTNAME
              value: "analytics-service"
            - name: MYSQL_HOST
              value: "mysql"
          ports:
            - containerPort: 8085
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 40
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: revplay
spec:
  selector:
    app: analytics-service
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085
