apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: revplay-user-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8081
          env:
            - name: EUREKA_HOSTNAME
              value: user-service
            - name: EUREKA_URI
              value: http://discovery-server:8761/eureka/
            - name: CONFIG_SERVER_URL
              value: http://config-server:8888/
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://mysql:3306/revplay_db?createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false
            - name: SPRING_DATASOURCE_USERNAME
              value: revplay_user
            - name: SPRING_DATASOURCE_PASSWORD
              value: revplay_password
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
